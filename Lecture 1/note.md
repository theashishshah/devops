1. Linux is a kernel and it is made in C++.


## Hypervisor (type 1): bare metal
- It will bring own its's own kernel

## Hypervisor (type 2):
- It doesn't bring own its' own kernel instead uses host OS and then divide the resources.

## Let's spin a VM (AWS): create own VM
- spin a EC2 machine and can connect to terminal


## cmd lines:
- cat: print file's data on terminal
- chmod 400
- ssh name@ip  (do ssh)
- sudo apt update ( just list )
- sudo apt upgrade ( to install actual packages)
- cat /etc/os-release ( to see os details)
- pwd (print working dir)
- ls -l (we can pass flag)
- ls -la
- ls -lh
- man cmdline to (:q) to quite
- cd - (to go back in a dir)
- cd ~ (home/abs dir)
- cp source file/folder destination file/folder (copies)
- mv source  destination/ (move and can rename)  (m)
-  mv log.txt log.js (rename)
- mkdir chai && cd chai (multiple cmd line using &&)
- find config.js (find the file)
- tree (shows tree strcuture)

